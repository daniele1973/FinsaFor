@using FinsaWeb.Models
@model ControlloAllievi
<style>
    .text-danger {
        color: red;
    }
</style>
@*<link href="~/css/site.css" type="text/css" />*@
<form asp-action="Elimina" method="post" class="form-horizontal" role="form">
    <div asp-validation-summary="All" class="text-danger"></div> @*Tag Helper. Tag con gli steroidi. Esegue codice lato server. --> In qs div stamperemo tutti i messaggi di errore relativi a questa form*@

    <div class="form-group">
        @*Bootstrap*@
        <label asp-for="IdStudente" class="col-md-2 control-label"></label> @*NB: la label non definisce un testo! Siccome è la label per il campo Name, di default ci mette il nome della proprietà. Se per es è in CamelCase e vuoi stampare altro c'è modo di dirglielo*@
        <div class="col-md-5">
            <input asp-for="IdStudente" class="form-control"></input>@*quello che metti nel campo finisce magicamente nella proprietà Name dell'obj di classe Feedback che gli abbiamo passato come Model. Alla fine non è che la form fa il post dell'obj FeedBack, manda una POST normale, ma ricalca le proprietà dell'obj di classe Feedback+
                +*@
            <span asp-validation-for="IdStudente" class="text-danger"></span> <!-- se gli passo un Model che ha una proprietà Name, la prende e la mette qui come valore predefinito di questo campo della form-->
        </div>
    </div>

    <input type="submit" />

</form>